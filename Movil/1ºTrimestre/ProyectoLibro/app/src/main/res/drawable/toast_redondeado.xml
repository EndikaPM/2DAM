<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Color del fondo -->
    <solid android:color="@color/white"/>

    <!-- Bordes redondeados -->
    <corners android:radius="20dp"/>

    <!-- Borde exterior -->
    <stroke
        android:width="5dp"
        android:color="@color/blueGrenDark"/>

    <!-- Espaciado interior -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp"/>

</shape>
